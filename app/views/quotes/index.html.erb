<main class="container">
  <div class="header">
    <h1>Quote</h1>
    <%=  link_to "New Quote", new_quote_path,
    class:"btn btn-primary" %>
  </div>
  <!-- Quote list -->
  <%= render @quotes %>
</main>